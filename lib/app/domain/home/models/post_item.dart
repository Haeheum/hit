class PostItem {
  PostItem({
    required this.title,
    required this.manager,
    required this.content,
    required this.date,
  });

  String title;
  String manager;
  String content;
  DateTime date;
}
